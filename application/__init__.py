# Marks this directory as a Python package.

# This allows modules inside the directory to be imported
# using package syntax, for example:

#     from application import config
#     from application.routers import auth